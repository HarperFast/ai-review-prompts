# Calibration log

Reverse-chronological log of weekly review-prompt calibration. Each entry summarizes the prior week's calibration signal from [HarperFast/ai-review-log](https://github.com/HarperFast/ai-review-log) and the prompt changes (if any) it motivated. Generated by the weekly calibration-feedback routine.

## Week of 2026-07-27

Sources: raw verdict-labeled issues from [HarperFast/ai-review-log](https://github.com/HarperFast/ai-review-log) (pre-fetched, `closed_at` in `[2026-07-27, 2026-08-03)`). The curated `Calibration log` ([#1086](https://github.com/HarperFast/ai-review-log/issues/1086)) and `False-negative log` ([#1085](https://github.com/HarperFast/ai-review-log/issues/1085)) supplements exist for this week but carry only their boilerplate seed bodies — no daily comments — so, as last week, they contributed nothing. **66 `verdict:pending` issues remain open**, up from 45: triage continues to fall further behind ingest, so this is again a partial read weighted toward whatever the batch reached.

### Verdict mix

**53 issues triaged: useful 34 · noise 3 · partial 16** (64% useful) — up from 56% last week and 53% the week before, the third consecutive improvement, though still short of the 83–85% pre-Actions era. Noise nearly vanished (7 → 3); partials grew in absolute terms (10 → 16) and are now the entire deficit.

Per model:

| Model | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| claude-sonnet-5 | 41 | 27 | 2 | 12 |
| claude-sonnet-4-6 | 8 | 5 | 0 | 3 |
| gemini-3-flash-preview | 4 | 2 | 1 | 1 |

Per prompt ref:

| Prompt ref | Triaged | useful | noise | partial |
|------------|---------|--------|-------|---------|
| [`82c9484`](https://github.com/HarperFast/ai-review-prompts/commit/82c9484f6f2560be4a37032858311e429bccd56c) (2026-07-21) | 43 | 31 | 2 | 10 |
| [`9cf49d2`](https://github.com/HarperFast/ai-review-prompts/commit/9cf49d23cb046ecd6c87f3bba86dc3338d6998fb) (2026-06-29) | 9 | 3 | 0 | 6 |
| [`c3e65f5`](https://github.com/HarperFast/ai-review-prompts/commit/c3e65f577d331f120df29c859771df0b2d04f2e8) (gemini) | 1 | 0 | 1 | 0 |

Per repo: `harper` 41 (27/2/12) · `harper-pro` 9 (6/0/3) · `oauth` 2 (1/0/1) · `ai-review-prompts` 1 (0/1/0).

**This is the first week with a meaningful post-[#73](https://github.com/HarperFast/ai-review-prompts/pull/73) sample: 43 entries at `82c9484`, 72% useful / 23% partial**, against 33% useful / 67% partial for the 9 stragglers still running `9cf49d2`. That direction is encouraging and consistent with last week's prediction, but treat it as suggestive rather than causal — the `9cf49d2` cell (9) is below the ~15 floor, the two cells are not matched on repo or PR difficulty, and the whole comparison sits inside a triage backlog that grew by 21 this week.

Only the sonnet-5 (41) and `82c9484` (43) cells clear the floor. sonnet-4-6 (8) and gemini (4) do not, and **the model gap remains confounded and is still not a model-quality signal** — sonnet-5 drew `harper` core while sonnet-4-6 drew `harper-pro`. gemini is reported for completeness; it is provider-harness output, out of scope for Claude-prompt edits.

Data-quality notes for future readers: three issues ([#1039](https://github.com/HarperFast/ai-review-log/issues/1039), [#1037](https://github.com/HarperFast/ai-review-log/issues/1037), [#818](https://github.com/HarperFast/ai-review-log/issues/818)) carry **two** `prompt:*` labels, so per-ref counts above resolve those from the body's `Prompt ref:` line, not the labels; issue titles are unreliable against bodies; and #818 is a stale close of a 2026-07-02 run that landed in this window by `closed_at` only.

### Recurring NOISE patterns (→ no edit)

Only 3 noise calls, all heskew-confirmed, and all the same vacuous-run shape — a "no blockers" pass on a diff with no correctness or security surface — already named by the **"Mechanical, no-logic diffs"** bullet and its `<!-- review:no-log -->` rule:

- [#1073](https://github.com/HarperFast/ai-review-log/issues/1073) (ai-review-prompts#79, gemini) — an identifier-string bump (`claude-opus-4-8` → `claude-opus-5`) written up with four sections of run notes.
- [#1058](https://github.com/HarperFast/ai-review-log/issues/1058) (harper#1910) — a one-line addition to a config list; flagged borderline in triage.
- [#1050](https://github.com/HarperFast/ai-review-log/issues/1050) (harper#1899) — a type-only widening with no runtime effect.

#1050 is a second-and-third look at the type-only sub-shape whose carve-out was **shipped and then withdrawn** last week for being below the ≥2-independent bar. It stays withdrawn: last week's withdrawal turned on the two points being the same author fixing the same TS2345 regression in the same function, and #1050 does not clearly break that (harper#1899 is the same `recordCommitLatency` lineage). **Watch-listed again**, with the same improved framing for when a genuinely independent point appears: phrase it around the *public declaration surface and build status* (type-check passes, emitted output and declarations unchanged), not "no runtime effect." Two noise entries out of 53 is not a pattern worth spending prompt text on.

### Recurring PARTIAL / FALSE-NEGATIVE patterns (→ three edits)

Sixteen partials, and the dominant shape is unchanged from the last two weeks: a clean or near-clean run alongside an **open, unadjudicated peer finding** that the run either never surfaced or explicitly declined. What is new this week is that two *distinct, separately-actionable* classes finally cleared the ≥2-independent bar, and one of them is the opposite failure mode from the usual.

**1. Parity with a sibling call site treated as proof of correctness (new — 2 points, both human-corroborated and author-fixed).** → **prompt edit A**.

- [#1023](https://github.com/HarperFast/ai-review-log/issues/1023) (harper#1869) — an options bag forwarded to lmdb-js `remove()`, which takes a *positional* `ifVersionOrValue` second argument rather than the options object `put()` accepts, so the argument is silently misapplied on the LMDB engine. @cb1kenobi corroborated; the fix in `a44cf4c54` narrowed the call to the RocksDB-with-transaction case.
- [#1014](https://github.com/HarperFast/ai-review-log/issues/1014) (harper#1858) — `mtlsRequired` propagated onto one HTTP-version server but not its h1/h2 UDS mirror siblings (`server/http.ts:772`, `:811`), an mTLS bypass. gemini flagged both sites high; @kriszyp fixed in `a3b922126`.

Both runs reasoned from "this matches the adjacent pattern." Neither existing bullet covers it: the Harper storage-engine notes describe RocksDB/LMDB divergence in behavior, not in *callee signatures*, and nothing tells a reviewer to enumerate the remaining property set when a flag is copied onto a derived object. This is Harper-layer-specific (engine duality, UDS mirrors) → `harper/common.md`.

**2. Severity inflation on defects confined to test code (2 points, both peer-corroborated in the *opposite* direction — the peer rated the same finding lower).** → **prompt edit B**. The only inflation class in the set, and notable because every other pattern this week is deflation:

- [#1013](https://github.com/HarperFast/ai-review-log/issues/1013) (harper#1856) — a pooled-`Buffer`/`Float32Array` alignment hazard in three test assertions raised as a **blocker** where gemini rated the same three sites *medium* and no production path was affected. The finding was real and drove a fix (`b66d27cd3`), and @kriszyp has since approved the PR; the over-call is the severity, not the observation.
- [#1016](https://github.com/HarperFast/ai-review-log/issues/1016) (harper#1860) — an untested new monitor `CLOSED`/`releaseReadTxn()` branch raised as a **blocker** where @cb1kenobi filed the same gap himself at *Low*, and the PR merged without the test.

The edit sets both a ceiling and a floor, because the deflation half of this is also real: [#1054](https://github.com/HarperFast/ai-review-log/issues/1054) (harper#1906, merged) shows the reverse — the run declared "no observed gaps" and missed gemini's note that only the last of several async `put`s is awaited in `tableClearSecondaryIndex.test.js:46`, a flakiness race in the very regression test the PR added. So: test-code defects are worth *posting*, as Suggestions, and are not blockers — with an explicit carve-out that a test validating *nothing* (wrong mechanism, stubs the I/O it should exercise, vacuous) stays a finding under the Harper test-validity meta-check, and that an untested new branch which is itself security-critical stays a blocker under the existing second category. Counterexample honored, and it is the reason for that second carve-out: [#999](https://github.com/HarperFast/ai-review-log/issues/999) (harper#1842) — the untested `enforcesCheckPermission` gate was legitimately blocker-grade, because the branch is a security boundary rather than test-quality hygiene.

**3. Concurrent peer review the run never saw (promoted from watch-list — 2 points).** → **prompt edit C**. Last week this stood at a single point and was watch-listed with the note that "a one-line 're-check the threads immediately before posting' would be cheap." It now has a second:

- [#1017](https://github.com/HarperFast/ai-review-log/issues/1017) (carried) — reported "no prior PR comments/threads to reconcile" while gemini had posted three findings, two rated high, **3.5 minutes earlier**; the author confirmed both real.
- [#1016](https://github.com/HarperFast/ai-review-log/issues/1016) (harper#1860) — a clean run posted concurrently with gemini's high-priority synchronous-throw finding on the same commit; the next run re-raised it as a blocker and the author fixed it in `0e79b24f2`.

Both are timing races against the existing "Before reviewing: read the prior conversation" section rather than gaps in it — the run did read, just too early — so the edit is one bullet inside that section, not a new section. Worth noting for both points: the concurrently-missed finding turned out to be **real** each time, so this is not merely a bookkeeping/etiquette issue.

**4. Severity deflation on open peer findings (the bulk — 8 points, but a compliance gap, not an uncovered class).** Reinforce, no new text. Weighted by corroboration:

- _Human-corroborated (the ones that should carry weight):_ [#1060](https://github.com/HarperFast/ai-review-log/issues/1060) (harper#1913) is the strongest deflation in the set — a clean run, then @kriszyp filed CHANGES_REQUESTED hours later on an add/alter-role truthiness gate in `role_validation.ts` / `hdbTerms.ts` where falsey non-booleans (`0`, `''`, `null`) skip the permission loop, including a latent `super_user` variant, plus a primitive-`in` `TypeError` turning 400s into 500s; author-fixed across `54962ed0c`…`95e9caa03`. [#1091](https://github.com/HarperFast/ai-review-log/issues/1091) (harper#1963) declined two open findings in `resources/auditStore.ts` — gemini HIGH at `:230` (unhandled rejection from an earlier per-loop delete's `committed` promise) and @cb1kenobi at `:248` (a throw in `finally` before `resolve()` re-opening the exact permanent wedge the PR set out to close). [#1057](https://github.com/HarperFast/ai-review-log/issues/1057) (harper-pro#608) surfaced neither of two gemini findings, one of which (`blockCacheEviction.test.mjs` teardown) @cb1kenobi acknowledged as valid. [#1088](https://github.com/HarperFast/ai-review-log/issues/1088) (harper#1958) was drafted `noise` and **overridden to `partial`** by heskew: the run verified the `react-native-fs` override / Dockerfile change substantively but missed that registry installs *do* honor `npm-shrinkwrap.json` (making the override-plus-stub the wrong shape) and that a hardcoded ~15-directory prune list will silently rot; the PR was closed unmerged for those reasons.
- _Bot-only / unadjudicated (weighted down accordingly):_ [#1083](https://github.com/HarperFast/ai-review-log/issues/1083) (harper#1952) — four gemini threads unresolved with zero human review on the PR; the run did engage the HIGH "rolling-restart stall" thread and argued a false premise from `threadServer.js`/`connectedPorts`, then conceded it was "worth a maintainer glance." [#1080](https://github.com/HarperFast/ai-review-log/issues/1080) (harper-pro#618) and [#1054](https://github.com/HarperFast/ai-review-log/issues/1054) — gemini MEDIUMs never surfaced or waved through as by-design.

Critically, **#1060 and #1091 are exactly what #73's "Severity deflation on security / hardening paths" bullet and last week's "a new guard is only as good as whatever populates the value it reads" bullet already describe** — a truthiness gate on a permission path, and a `finally`-throw that skips the resolve (also already named in the setup/teardown bullet). #1060 ran post-`82c9484`, so this is the text failing to *bind*, not missing text. Adding a fourth restatement would dilute rather than fix. The honest read: the deflation language is present and correct, and the remaining lever is the "don't decline an open peer finding without adjudicating it" discipline — which the concurrent-review edit (C) partially serves by making the peer's finding visible at posting time in the first place.

**Covered / one-off, no edit:**

- [#1043](https://github.com/HarperFast/ai-review-log/issues/1043) — gemini re-raised a finding a maintainer had already dismissed. Already covered by the "honor dismissals from the prior conversation" lines in `universal.md`; a compliance gap in provider-harness output, out of scope.
- [#1049](https://github.com/HarperFast/ai-review-log/issues/1049) — a false positive on tracked-object spread behavior. Single PR, and the existing `GenericTrackedObject` caveat in `harper/common.md` ("verify against the specific tracked type rather than assuming either way") is already the correct instruction; the run didn't follow it. No edit.
- [#993](https://github.com/HarperFast/ai-review-log/issues/993) (harper#1835) — a `.opened` self-clear-guard blocker that is plausible but bot-only, still unresolved, on an open draft. Real-looking, uncorroborated; no pattern.
- [#1077](https://github.com/HarperFast/ai-review-log/issues/1077) (harper-pro#615) — drafted `useful`, corrected to `partial` mostly for **review staleness**: the only run covers the first spec, and the PR later gained `4e7b7ce1a` and `4b186a343` with no re-review. This is a trigger/harness concern, not addressable in prompt text — flagged here so it isn't mistaken for a prompt gap.

### Prompt changes this week

Three targeted edits, each backed by ≥2 independent points with peer or human corroboration:

1. **`harper/common.md` → "Review checks specific to Harper": "Parity with a sibling call site is not proof of correctness."** New bullet covering the two sub-shapes — verify the callee's real signature on the *other* storage engine before mirroring an argument (lmdb-js `remove(key, ifVersionOrValue)` is positional, unlike `put()`), and enumerate the rest of the property set when propagating a flag onto a derived/mirrored object (UDS/mirror servers, cloned configs). Cites harper#1869 and harper#1858.
2. **`universal.md` → Testing: defects confined to test code are Suggestions, not blockers**, and the four NEW-code categories are the *whole* list of missing-coverage blockers — with carve-outs for tests that validate nothing and for untested security-critical branches. Cites harper#1856 and harper#1860.
3. **`universal.md` → "Before reviewing: read the prior conversation": re-read the threads immediately before you post.** Re-fetch `reviewThreads` right before commenting and reconcile anything that landed mid-trace. Cites ai-review-log#1017 and harper#1860.

No `repo-type/*.md` edits: nothing this week was specific to a repo *type* (edit 1 is Harper-engine-specific, edits 2 and 3 are cross-repo reviewer discipline).

Watch-list carried forward: the type-only-diff carve-out, still below the ≥2-independent bar ([#1050](https://github.com/HarperFast/ai-review-log/issues/1050), [#1040](https://github.com/HarperFast/ai-review-log/issues/1040), [#1011](https://github.com/HarperFast/ai-review-log/issues/1011)); "don't decline an open peer finding without adjudicating it" as a possible explicit rule if #73's deflation language keeps failing to bind ([#1060](https://github.com/HarperFast/ai-review-log/issues/1060), [#1091](https://github.com/HarperFast/ai-review-log/issues/1091), [#1083](https://github.com/HarperFast/ai-review-log/issues/1083)); review staleness on PRs that keep moving after a run ([#1077](https://github.com/HarperFast/ai-review-log/issues/1077)) — a harness concern, not prompt text; reviewer-tool config as an ignorable surface ([#1026](https://github.com/HarperFast/ai-review-log/issues/1026)); and an awaited call outside the recovery `try` ([#978](https://github.com/HarperFast/ai-review-log/issues/978)) — neither of the last two recurred. The concurrent-peer-review race is **off** the watch-list, promoted to edit 3.

_(Open PRs at time of writing: [#81](https://github.com/HarperFast/ai-review-prompts/pull/81), which touches only `evals/*` — no overlap with these edits, and the natural future validator for them; and [#18](https://github.com/HarperFast/ai-review-prompts/pull/18), a workflow change, unrelated.)_

## Week of 2026-07-20

Sources: raw verdict-labeled issues from [HarperFast/ai-review-log](https://github.com/HarperFast/ai-review-log) (pre-fetched, `closed_at` in `[2026-07-20, 2026-07-27)`). The curated `Calibration log` ([#1046](https://github.com/HarperFast/ai-review-log/issues/1046)) and `False-negative log` ([#1045](https://github.com/HarperFast/ai-review-log/issues/1045)) supplements exist for this week but carry only their boilerplate headers — no daily comments — so they contributed nothing. **45 `verdict:pending` issues remain open**, up from 13 a week ago: triage is falling behind ingest, so this week's mix is a partial read weighted toward whatever the batch reached.

### Verdict mix

**39 issues triaged: useful 22 · noise 7 · partial 10** (56% useful) — essentially flat against last week's 53%, and still well below the 83–85% of the pre-Actions era. Verdicts again arrived as one bulk batch (all applied 2026-07-21/22 except five late `useful` auto-applies on 07-24/25).

Per model:

| Model | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| claude-sonnet-5 | 23 | 16 | 4 | 3 |
| claude-sonnet-4-6 | 10 | 4 | 0 | 6 |
| gemini-3-flash-preview | 6 | 2 | 3 | 1 |

Per prompt ref:

| Prompt ref | Triaged | useful | noise | partial |
|------------|---------|--------|-------|---------|
| [`9cf49d2`](https://github.com/HarperFast/ai-review-prompts/commit/9cf49d23cb046ecd6c87f3bba86dc3338d6998fb) (2026-06-29) | 30 | 16 | 5 | 9 |
| [`82c9484`](https://github.com/HarperFast/ai-review-prompts/commit/82c9484f6f2560be4a37032858311e429bccd56c) (2026-07-21) | 6 | 5 | 0 | 1 |
| _(none recorded — gemini)_ | 3 | 1 | 2 | 0 |

Per repo: `harper` 24 (16/4/4) · `oauth` 10 (3/3/4) · `harper-pro` 5 (3/0/2).

**The prompt-ref split is the load-bearing fact this week.** `9cf49d2` predates [#73](https://github.com/HarperFast/ai-review-prompts/pull/73) (merged as `aa26ae2`, 2026-07-17), so it carries **neither** the "Severity deflation on security / hardening paths" nor the "Fail-closed / validate-at-registration on new surfaces" bullet; `82c9484` carries both. Nine of the ten partials ran at `9cf49d2` — they are pre-text misses, not evidence the text failed. The post-text cell is only 6 entries, far below the ~15 floor, so **no before/after read is possible yet**; it is also unbalanced (5 of 6 are `harper` sonnet-5 clean runs from a single day). Next week should be the first with a meaningful post-`aa26ae2` sample.

The sonnet-5 canary cell (23) clears the floor at 70% useful / 13% partial; the sonnet-4-6 cell (10) does not, and reads 40% / 60%. As last week, **this gap is confounded and is not a model-quality signal** — sonnet-5 reviewed `harper` core PRs while sonnet-4-6 drew the `oauth` security work and `harper-pro`, where second-reviewer density is much higher. The gemini cell (6) is reported for completeness; gemini is provider-harness output, out of scope for Claude-prompt edits.

### Recurring NOISE patterns (→ one small edit)

All 7 noise calls are the same family — a "no blockers" run (three with full run-notes) on a diff with no correctness or security surface — and every one was human-confirmed by heskew. Four are already named by the **"Mechanical, no-logic diffs"** bullet: a `pull_request` node-matrix conditional in a workflow ([#1032](https://github.com/HarperFast/ai-review-log/issues/1032)), a CI-lane workflow sync ([#1024](https://github.com/HarperFast/ai-review-log/issues/1024)), and two release bumps touching only `CHANGELOG.md`/`package.json`/lockfile ([#1027](https://github.com/HarperFast/ai-review-log/issues/1027), [#1028](https://github.com/HarperFast/ai-review-log/issues/1028)).

The one arguably-new sub-shape is **type-only diffs with no runtime effect** ([#1040](https://github.com/HarperFast/ai-review-log/issues/1040), [#1011](https://github.com/HarperFast/ai-review-log/issues/1011)) — but review feedback on this PR established these are NOT independent points: both map to the same author fixing the same TS2345 regression in the same function (`resources/DatabaseTransaction.ts::recordCommitLatency`, harper#1853 merged / harper#1890 closed unmerged, both fallout of harper#1688). One underlying signal → below the ≥2-independent bar → **watch-listed, no prompt edit**. When an independent occurrence appears, the edit should be phrased around the *public declaration surface and build status* (type-check passes, emitted output and declarations unchanged), not just "no runtime effect" — a type-only diff can still break `tsc` or shift an inferred exported type.

Two single-point shapes are **watch-listed, no edit**: reviewer-tool configuration as a diff surface (`.gemini/config.yaml` + `.gemini/styleguide.md`, [#1026](https://github.com/HarperFast/ai-review-log/issues/1026)) — adjacent to "inert CI-config diffs" but not a workflow; and a release PR whose run re-traced the security fixes of the *sibling* PRs it bundles instead of its own +12/-3 diff ([#1028](https://github.com/HarperFast/ai-review-log/issues/1028)) — the existing "do not manufacture run-notes that restate the PR body" line already points at this, so reinforce rather than add.

### Recurring PARTIAL / FALSE-NEGATIVE patterns (→ one edit; the rest covered or pre-text)

Every partial is the same structural shape: a clean "no blockers" (or near-clean) run while a second reviewer — gemini inline, then a human (@kriszyp, heskew, cb1kenobi) who in most cases *fixed* it — caught a real defect on the reviewed code. Corroboration this week is strongly human/author-fixed rather than bot-only: eight of the ten partials name a specific author commit that landed the fix.

**1. Verified the enforcing side of a new guard, never the producing side (new — 3 points, all `oauth`, all three author-fixed).** The week's one genuinely uncovered class:

- [#1019](https://github.com/HarperFast/ai-review-log/issues/1019) (oauth#183) — the run traced the callback-side `sessionId` CSRF binding exhaustively (ordering, backward-compat, all four tests) and passed it clean; the MCP *initiation* path never mints `sessionId` into the token, so the new check silently no-ops for MCP flows. Author-fixed `300b06bed`.
- [#1021](https://github.com/HarperFast/ai-review-log/issues/1021) (oauth#184) — the run enumerated `dcrEnabled()`'s cases and declared "all three cases correct"; a bare YAML `dynamicClientRegistration:` parses to `null`, which `dcrConfig !== undefined` treats as configured. Author-fixed `01a6f428b` ("fail closed instead of TypeError").
- [#1042](https://github.com/HarperFast/ai-review-log/issues/1042) (oauth#192) — the run asserted an env-expanded `"false"` "cannot activate the gate"; @kriszyp's inline shows an *unexpanded* `${VAR}` placeholder survives `coerceConfigBoolean` as a truthy string and silently switches on a documented default-off gate. Author-fixed `160992d4` (boolean config normalization made total; oauth#192 merged 2026-07-23).

The common failure is treating a gate's internal case analysis as sufficient without asking who writes the value it reads. Not covered by "Dispatch surfaces" (wrappers), "Fail-closed / validate-at-registration" (rejecting malformed input at a *new* surface), or "Falsy vs nullish guards" (which names the `!= null` fix but not the producer-side trace that surfaces the need for it). → **prompt edit** (below).

**2. Severity deflation on peer-flagged findings (5 points — but pre-text).** [#987](https://github.com/HarperFast/ai-review-log/issues/987) (the give-up abort path leaks native handles for other chain links; @kriszyp-fixed `6be2c12d6`, then `e0e78f003`), [#942](https://github.com/HarperFast/ai-review-log/issues/942) (`Number(' ')` → `0` whitespace `Retry-After` on a function the run called "all correct"; heskew: "Good catch — taken in `cb7de4825`"), [#1035](https://github.com/HarperFast/ai-review-log/issues/1035) (reprobe close-handling declared complete and "No gaps"; @kriszyp added the `if (closed) return` guard and the `shouldCloseSendAuthWatch` refactor), [#1009](https://github.com/HarperFast/ai-review-log/issues/1009) (caught the real untested `generate: true` branch, but explicitly called `generate`+`key` both-supplied benign where gemini rated it HIGH, and missed `execFileSync` blocking the event loop; both author-fixed in `d30fb7f`), and [#1017](https://github.com/HarperFast/ai-review-log/issues/1017) (global `Transaction.prototype.commit` stub with no `afterEach` restore; author-fixed `8c622e764`). **All five ran at `9cf49d2`** — before the severity-deflation and fail-closed bullets existed. #987 and #942 are also carry-overs re-adjudicated after post-close re-reviews, so they are not fresh independent points. **Reinforce, no new text**; the honest test of #73's language is next week's post-`aa26ae2` sample.

**3. Concurrent peer review the run never saw (1 point, watch-listed).** [#1017](https://github.com/HarperFast/ai-review-log/issues/1017) reported "no prior PR comments/threads to reconcile" while gemini had posted three findings — two rated high — **3.5 minutes earlier**; the author confirmed both real. This is a timing race against the existing "Before reviewing: read the prior conversation" section rather than a gap in it (the run did read, just too early). A one-line "re-check the threads immediately before posting" would be cheap, but it stands at a single point — **watch-listed**.

**Covered / one-off, no edit:** [#978](https://github.com/HarperFast/ai-review-log/issues/978) — `await Promise.all(checkpointWrites)` sitting outside the top `try` can bypass the park path (@kriszyp, unresolved on a `CHANGES_REQUESTED` PR); adjacent to "Setup/teardown that leaks on partial failure", 1 point, watch-listed. [#1020](https://github.com/HarperFast/ai-review-log/issues/1020) is gemini severity *inflation* — doc-only drift filed as two blockers — and is provider-harness output, out of scope; note though that the underlying drift is real and shipped (`README.md` on merged `oauth` main still advertises `/oauth/mcp/register` as "open registration" after the fail-closed change), and the Claude run on the same PR ([#1021](https://github.com/HarperFast/ai-review-log/issues/1021)) missed it too — a second point for "docs must follow a behavior change", which the existing blocker list already names.

### Prompt changes this week

One targeted addition to `universal.md`, backed by 3 independent, human-confirmed, all-author-fixed points:

1. **Security — "A new guard is only as good as whatever populates the value it reads."** New bullet after the fail-closed one: when a PR adds an enforcement check, enumerate every path that *produces* the value it keys on (including paths that never set it, and shapes the config layer can actually emit) before calling the gate correct. Cites oauth#183 / #184 / #192 from the three partials above.

_(A second edit — a type-only-diff carve-out under "Mechanical, no-logic diffs" — shipped in this PR's first revision and was withdrawn on review: its two supporting points turned out to be the same underlying fix, below the ≥2-independent bar. Watch-listed above with the improved declaration-surface framing for when an independent point appears.)_

No `harper/*.md` or `repo-type/*.md` edits: the `oauth` cluster is a plugin-agnostic guard-design shape, not Harper- or plugin-layer-specific.

Watch-list carried forward: the concurrent-peer-review timing race ([#1017](https://github.com/HarperFast/ai-review-log/issues/1017)); reviewer-tool config as an ignorable surface ([#1026](https://github.com/HarperFast/ai-review-log/issues/1026)); an awaited call outside the recovery `try` ([#978](https://github.com/HarperFast/ai-review-log/issues/978)); plus last week's `.pipe()`→`stream.pipeline()` idiom, the param-routing / trailing-slash cluster, and the `node -e` argv re-raise behavior — none of which recurred this week.

(Open PRs at time of writing: only [#18](https://github.com/HarperFast/ai-review-prompts/pull/18), a workflow change unrelated to calibration. No prior calibration PR is open, so there is nothing to duplicate or contradict — #73, #77 and #78 have all merged.)

## Week of 2026-07-13

Sources: raw verdict-labeled issues from [HarperFast/ai-review-log](https://github.com/HarperFast/ai-review-log) (pre-fetched). The curated `Calibration log` / `False-negative log` supplements were empty for this week. This is the first sweep run from the GitHub Actions runner (successor to the disabled claude.ai routine); the week's signal arrived as a single bulk triage batch (all verdicts applied 2026-07-16/17) rather than daily sweeps, and **13 `verdict:pending` issues remain open**, so the mix below is a partial read of the week. _(Counts are the 2026-07-17 snapshot; post-snapshot churn — e.g. [#942](https://github.com/HarperFast/ai-review-log/issues/942) reopened for re-review after fresh runs, late auto-applies — shifts individual cells by ±1 and rolls into the following week's window.)_

### Verdict mix

**78 issues triaged: useful 41 · noise 10 · partial 27** (53% useful) — a markedly more partial-heavy mix than recent weeks (83–85% useful), consistent with a back-log catch-up batch that concentrated on the harder, second-reviewer-corroborated cases rather than the usual daily "no blockers" baseline volume.

Per model:

| Model | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| claude-sonnet-5 | 40 | 30 | 1 | 9 |
| claude-sonnet-4-6 | 28 | 9 | 4 | 15 |
| gemini-3-flash-preview | 10 | 2 | 5 | 3 |

Per prompt ref: **single-ref week** — every Claude entry ran at `9cf49d2` (baseline phase); no before/after read is possible. (Four entries carry no `Prompt ref` field: three gemini noise runs + one gemini partial.)

Both Claude cells clear the ~15-entry reporting floor, so the counts stand — but the sonnet-5 vs sonnet-4-6 partial-rate gap (22% vs 54%) is **confounded and must not be read as a model-quality signal**: the two models reviewed different PRs (sonnet-5 → recent `harper` core PRs; sonnet-4-6 → mostly `harper-pro` + `oauth`), not a controlled A/B, so the gap reflects PR/repo difficulty mix as much as anything. The gemini cell (10) is below the floor — reported for completeness only; gemini is provider-harness output, out of scope for Claude-prompt edits.

### Recurring NOISE patterns (→ none to add)

9 of 10 noise calls are the same already-covered mode — a full "no blockers" run on a diff with no correctness/security surface: a CI `retention-days` tweak ([#1000](https://github.com/HarperFast/ai-review-log/issues/1000), sonnet-5), a `package.json`/lockfile dependency repin ([#922](https://github.com/HarperFast/ai-review-log/issues/922)), a Dockerfile base-image bump ([#932](https://github.com/HarperFast/ai-review-log/issues/932)), a docs-only README + patch bump ([#935](https://github.com/HarperFast/ai-review-log/issues/935)), and five gemini `oauth` runs on the same mechanical/docs surfaces ([#974](https://github.com/HarperFast/ai-review-log/issues/974), [#972](https://github.com/HarperFast/ai-review-log/issues/972), [#970](https://github.com/HarperFast/ai-review-log/issues/970), [#934](https://github.com/HarperFast/ai-review-log/issues/934), [#933](https://github.com/HarperFast/ai-review-log/issues/933)). All named by the existing **"Mechanical, no-logic diffs"** bullet + `<!-- review:no-log -->` marker.

The one distinct call is a **confidently-wrong false positive**: a sonnet-4-6 run blocked `sync-core.sh:50` claiming `node -e`'s `process.argv.slice(1)` is off-by-one, then **re-raised it in a later run despite the author (@kriszyp) disputing it with empirical `node -e` argv output** ([#947](https://github.com/HarperFast/ai-review-log/issues/947), human-confirmed noise — `node -e 'code' a b c` yields `[node, a, b, c]` with no placeholder at `argv[1]`, so `.slice(1)` is correct). Single point, and narrowly specific (both the `node -e` argv semantics and the re-raise-without-engaging-counter-evidence behavior) → **watch-listed, no edit**.

### Recurring PARTIAL / FALSE-NEGATIVE patterns (→ none to add; all covered or pending)

Nearly every partial is the familiar shape — Claude (or gemini) posted a bare "no blockers" while a second reviewer (gemini inline, then a human — @kriszyp, heskew, cb1kenobi — who often fixed it) caught a real defect on the reviewed code. Three corroborated clusters, **each already named by current or in-flight prompt text**:

1. **Numeric coercion + falsy/nullish guards** (dominant, ~6 points, cross-model, mostly human/author-fixed). `Number(' ')` → `0` on a whitespace `Retry-After` ([#942](https://github.com/HarperFast/ai-review-log/issues/942), heskew-fixed), `Number()` mishandling ISO-date/boolean `@expiresAt` values the run had *blessed* as falling back to default ([#975](https://github.com/HarperFast/ai-review-log/issues/975), kriszyp-fixed), an unvalidated `Number()` parse ([#954](https://github.com/HarperFast/ai-review-log/issues/954), kriszyp "good catch"), an `if (AGGREGATE_PERIOD)` truthiness guard passing `-1000` into a `setTimeout` clamp ([#982](https://github.com/HarperFast/ai-review-log/issues/982), kriszyp-fixed), a `'0'`-is-falsy `PARK_WARN_MS` guard ([#995](https://github.com/HarperFast/ai-review-log/issues/995), author-fixed), and a truthiness guard dropping an explicit `null` override that should have been `!= null` ([#967](https://github.com/HarperFast/ai-review-log/issues/967), maintainer-fixed). This is **exactly** the territory of the existing `universal.md` **"Falsy vs nullish guards"** and **"Numeric coercion and range on config / parsed / getter values"** bullets — both shipped within the last two cycles (ref `9cf49d2`). These are same-week recurrences of freshly-live text, not a new blind spot; the conservative call is to let the text run a cycle rather than pile on. **Reinforce, no new text.**

2. **Abort / teardown / handle leaks on the failure branch** (human/author-fixed). Native transaction handles/snapshots leaked when only the current link (not the `this.next` chain) is aborted on give-up ([#987](https://github.com/HarperFast/ai-review-log/issues/987), kriszyp-fixed `abortChainAfterRetries`), an unconsumed `/user/emails` HTTP response body ([#962](https://github.com/HarperFast/ai-review-log/issues/962), author-fixed `body?.cancel()`), a `Promise.all` partial-startup teardown leak + unhandled `fetch` rejection ([#992](https://github.com/HarperFast/ai-review-log/issues/992), author-fixed), and a bare `.pipe()` blessed where `stream.pipeline()` was needed for error propagation/teardown ([#953](https://github.com/HarperFast/ai-review-log/issues/953), kriszyp-fixed). Covered by the existing **"Setup/teardown that leaks on partial failure"** and **"Error and abort paths that crash or leak"** bullets. The `.pipe()`→`stream.pipeline()` idiom is a crisp, nameable shape but stands at 1 point → **watch-listed**; otherwise reinforce, no new text.

3. **Severity deflation / fail-open on security & hardening surfaces** (`oauth`-heavy, human-HIGH). A rate limiter ordered *before* auth → unauthenticated quota exhaustion ([#930](https://github.com/HarperFast/ai-review-log/issues/930), heskew fix: move post-auth), a cleartext `http:` issuer + non-atomic JTI replay store on the token endpoint ([#924](https://github.com/HarperFast/ai-review-log/issues/924), heskew HIGH, author-fixed), and a `try/catch` swallowing a missing `nm` binary → silently **bypassing the whole ABI-enforcement gate** (fail-open) ([#937](https://github.com/HarperFast/ai-review-log/issues/937), gemini HIGH, author-fixed). This overlaps the **still-open, unmerged** additions in [PR #73](https://github.com/HarperFast/ai-review-prompts/pull/73) ("Severity deflation on security / hardening paths" and "Fail-closed / validate-at-registration on new surfaces") — those bullets are **not yet in ref `9cf49d2`**, which is precisely why these reviews still missed them. Per the "don't duplicate an open calibration PR's edits" rule, this entry **notes the overlap and adds nothing**; #73 already targets this class.

**Corroborated-but-already-covered / one-offs (no edit):** a partial optional-chain `TypeError` (`custody?.getPublicKey().fingerprint`, [#957](https://github.com/HarperFast/ai-review-log/issues/957)) and a non-array `sendsTo`/`receivesFrom` crash + replication regressions ([#945](https://github.com/HarperFast/ai-review-log/issues/945)) sit under **"Unvalidated shape"**; several test-validity/robustness under-calls ([#952](https://github.com/HarperFast/ai-review-log/issues/952), [#955](https://github.com/HarperFast/ai-review-log/issues/955), [#954](https://github.com/HarperFast/ai-review-log/issues/954)) under the existing **"Test validity"** meta-check; a multi-trailing-slash URL-normalization gap ([#939](https://github.com/HarperFast/ai-review-log/issues/939), heskew-fixed) is a second point for the param-routing cluster watch-listed on 2026-06-22 — still `harper`-routing-specific, **watch-listed**; and single-point one-offs — a notification-logic gap ([#946](https://github.com/HarperFast/ai-review-log/issues/946)), an `npm prune --omit=dev` no-op for a generated shrinkwrap ([#944](https://github.com/HarperFast/ai-review-log/issues/944)), `sha256sum` macOS portability ([#948](https://github.com/HarperFast/ai-review-log/issues/948)), config-AST validation ([#981](https://github.com/HarperFast/ai-review-log/issues/981)). One "partial" is correctly classified not as a false negative but as a real finding **left unresolved** ([#976](https://github.com/HarperFast/ai-review-log/issues/976)). The three gemini partials ([#964](https://github.com/HarperFast/ai-review-log/issues/964) severity inflation on a version bump belonging to a sibling release PR; [#940](https://github.com/HarperFast/ai-review-log/issues/940) and [#923](https://github.com/HarperFast/ai-review-log/issues/923), gemini peers of #939/#924) are provider-harness output, out of scope.

Signal weighting: the corroboration this week is unusually human-heavy — @kriszyp, heskew, and cb1kenobi confirmed and in most cases fixed the numeric, abort-leak, and security points; gemini-only items are the minority and reinforce classes the humans independently fixed rather than standing alone.

### Prompt changes this week

**None — log-only week.** Every recurring, corroborated class is already named by live `universal.md` text (falsy-vs-nullish guards, numeric coercion and range, setup/teardown leaks, error/abort paths) or by the still-open [PR #73](https://github.com/HarperFast/ai-review-prompts/pull/73) security-deflation / fail-closed additions. The week's misses are same-week recurrences of freshly-shipped (or not-yet-merged) guidance, not new blind spots — so the conservative call per the rubric is to add nothing and watch whether next week's mix moves once that text has run a full cycle (and once #73/#77 merge). Watch-list carried forward: the `.pipe()`→`stream.pipeline()` idiom (#953), the param-routing / trailing-slash cluster (#939, now 2 points across weeks), and the `node -e` argv false-positive + re-raise-without-engaging-counter-evidence behavior (#947).

(Open calibration PRs at time of writing: [#73](https://github.com/HarperFast/ai-review-prompts/pull/73) week of 2026-06-29 — `universal.md` edits, unmerged; [#77](https://github.com/HarperFast/ai-review-prompts/pull/77) week of 2026-07-06 — log-only. Neither is duplicated or contradicted here.)

## Week of 2026-07-06

Primary source: the week's verdict-labeled issues in [HarperFast/ai-review-log](https://github.com/HarperFast/ai-review-log), pre-fetched directly (raw labels, per the successor routine). Supplements: [Calibration log #846](https://github.com/HarperFast/ai-review-log/issues/846) · [False-negative log #870](https://github.com/HarperFast/ai-review-log/issues/870) — both present but carry only their scaffolding headers this week (the rolling daily-summary routines that populate them have stalled), so all synthesis below is from raw labels.

### Verdict mix

**102 issues triaged: useful 98 · noise 3 · partial 1** (96% useful). 13 `verdict:pending` issues remain open, so the week's signal is largely but not fully settled. A very clean, useful-heavy week concentrated in one large 07-10 verdict sweep; findings were sparse (10 of the 98 useful entries carried an actual blocker/finding, the other 88 were genuine "no blockers" passes).

**Per model:**

| Model | useful | noise | partial | total |
|-------|--------|-------|---------|-------|
| claude-sonnet-4-6 | 94 | 1 | 1 | 96 |
| gemini-3-flash-preview | 4 | 2 | 0 | 6 |

No `claude-sonnet-5` canary entries landed this week — the harper canary produced no triaged verdicts, so there is no sonnet-5-vs-sonnet-4-6 read to report. All 94 Claude entries ran on `claude-sonnet-4-6`.

**Per prompt ref (short SHA):**

| Prompt ref | useful | noise | partial | total |
|------------|--------|-------|---------|-------|
| `9cf49d23cb04` (claude + gemini-on-oauth) | 98 | 1 | 1 | 100 |
| `d1d9a655e969` (gemini) | 0 | 1 | 0 | 1 |
| `accea2706fae` (gemini) | 0 | 1 | 0 | 1 |

The two non-`9cf49d23cb04` refs are the prompt SHAs the gemini reviewer ran against on this repo's own prompt-layer PRs (noise, below); there is no before/after prompt-change read to draw — `9cf49d23cb04` has been the live claude ref across the whole window.

**Per repo:** harper 69 (68 useful + 1 noise) · harper-pro 23 (all useful) · oauth 8 (7 useful + 1 partial) · ai-review-prompts 2 (both noise, gemini).

### Recurring NOISE patterns (→ none to add)

All three noise calls are already-covered shapes, and none is the review *over-flagging* — each is a vacuous "no blockers" on a diff with no reviewable surface:

1. **Lint-only mechanical diff** — [#847](https://github.com/HarperFast/ai-review-log/issues/847) (harper#1612): a +1/−1 `node:assert/strict` → `node:assert` import swap, the follow-through of the #1558 lint rule. No correctness surface; the clean run was a non-event. Covered by the existing **"Mechanical, no-logic diffs"** bullet + the `<!-- review:no-log -->` marker.
2. **Provider runs on prompt-prose PRs** — [#842](https://github.com/HarperFast/ai-review-log/issues/842) (ai-review-prompts#73) and [#820](https://github.com/HarperFast/ai-review-log/issues/820) (ai-review-prompts#72): gemini reviews of markdown-only prompt-layer PRs against this repo. Vacuous by construction — no code surface to verify — and the same convention already logged as [#670](https://github.com/HarperFast/ai-review-log/issues/670)/[#747](https://github.com/HarperFast/ai-review-log/issues/747). Both are gemini (bot-only) and doc-only regardless.

No new suppression text warranted; the existing rules already name these shapes. Nothing the review is over-flagging surfaced this week.

### Recurring PARTIAL / FALSE-NEGATIVE patterns (→ none new; reinforces open PR #73)

One partial this week:

1. **Severity deflation / under-calling on a clean review** — [#903](https://github.com/HarperFast/ai-review-log/issues/903) (oauth#167): Claude's single review returned "no blockers" while gemini's leg on the same PR ([#902](https://github.com/HarperFast/ai-review-log/issues/902)) caught a real finding the author then fixed (commit `2c1b2a9`; gemini's re-review confirmed "Prior 1 finding(s) resolved"). **Human-corroborated** (triaged by Nathan) **and author-fixed and peer-corroborated** — a strong single point, same class as the #699/#700 precedent.

Signal weighting: this is the highest-weight point of the week (human + author-fix + peer), but it is a *single* point and its specific technical sub-class isn't recorded in the triage note (the missed finding lives in the gemini #902 leg, outside the pre-fetched Claude-verdict set). It is the same "under-called clean review / severity deflation" class that the still-open **[PR #73](https://github.com/HarperFast/ai-review-prompts/pull/73)** (week of 2026-06-29, unmerged) already addresses by adding severity-*deflation* guidance to `universal.md`. Per the sweep rule on open prior calibration PRs, this week does **not** add competing or duplicate text — #903 corroborates #73's direction and should be weighed when that PR is reviewed.

Positive corroboration worth noting: on the oauth signing-key GC bug, Claude ([#852](https://github.com/HarperFast/ai-review-log/issues/852)) and gemini ([#851](https://github.com/HarperFast/ai-review-log/issues/851)) independently flagged the same blocker — the clean-review under-call in #903 is not a systemic collapse of Claude's calibration on oauth.

### Prompt changes this week

**No prompt changes this week.** The noise calls are all already-covered non-event shapes (nothing over-flagged), and the single false-negative (#903) is one data point in a class already handled by the open, unmerged PR #73 — below the "≥2 independent, ideally human-corroborated points, and don't duplicate an open PR" bar. A log-only week.

## Week of 2026-06-29

Sources: [Calibration log #748](https://github.com/HarperFast/ai-review-log/issues/748) · [False-negative log #772](https://github.com/HarperFast/ai-review-log/issues/772)

### Verdict mix

**138 issues triaged across six daily sweeps: useful 115 · noise 9 · partial 14** (83% useful). Signal concentrated in two large bulk sweeps (06-29 and 07-03); the other four days were low-volume and clean.

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 06-29 | 62 | 48 | 6 | 8 |
| 06-30 | 3 | 3 | 0 | 0 |
| 07-01 | 2 | 2 | 0 | 0 |
| 07-03 | 68 | 59 | 3 | 6 |
| 07-04 | 2 | 2 | 0 | 0 |
| 07-05 | 1 | 1 | 0 | 0 |

(Calibration log carried 06-29 → 07-05; false-negative log carried four daily scans 06-30 → 07-03.)

### Recurring NOISE patterns (→ none to add)

All 9 noise calls are the same already-covered mode: a full "no blockers" run on a diff with zero correctness/security surface — Dockerfile build-ARG ([#740](https://github.com/HarperFast/ai-review-log/issues/740)), a `HARPER_SAFE_MODE`-gated startup log ([#719](https://github.com/HarperFast/ai-review-log/issues/719)), comments-only docs ([#717](https://github.com/HarperFast/ai-review-log/issues/717)), an explicit "verification-only, do not merge" branch ([#697](https://github.com/HarperFast/ai-review-log/issues/697)), a log-level demotion ([#693](https://github.com/HarperFast/ai-review-log/issues/693)), a 2-line config-param registration ([#692](https://github.com/HarperFast/ai-review-log/issues/692)); and on 07-03 an additive optional `pathname?: string` type field ([#766](https://github.com/HarperFast/ai-review-log/issues/766)/[#765](https://github.com/HarperFast/ai-review-log/issues/765)) and a markdown/prompt-only diff ([#747](https://github.com/HarperFast/ai-review-log/issues/747)). All are fully named by the existing **"Mechanical, no-logic diffs"** bullet + the `<!-- review:no-log -->` marker in `universal.md`. The one arguably-newer shape — a type-only additive field with no runtime surface — is a single PR (two provider runs) and already sits within "no reviewable surface"; **watch-listed, no edit**.

### Recurring PARTIAL / FALSE-NEGATIVE patterns (→ edit)

The week's headline is a **severity-deflation** cluster that dominated both bulk sweeps' partials (5/8 on 06-29, 4/6 on 07-03), plus two corroborated false-negative classes. Signal weighting anchors on author-fixed / human-flagged points; gemini-only items reinforce rather than stand alone.

1. **Severity deflation on security / hardening paths (→ `universal.md`).** The dominant partial mode: a real, peer-flagged defect talked *down* to a clean pass with an unproven rationalization. Stale `context.user` called "safe for RBAC-level checks" — gemini rated CRITICAL, **author fixed** ([#775](https://github.com/HarperFast/ai-review-log/issues/775), harper#1535); a silent env-decrypt skip called correct, later **hardened warn→error** ([#760](https://github.com/HarperFast/ai-review-log/issues/760), harper#1528); CDP-hardening items declined as "no security escalation" / "not reachable in production" ([#786](https://github.com/HarperFast/ai-review-log/issues/786), harper#1547); a `tag_name` shell-interpolation under-called "negligible" vs gemini's blocker ([#699](https://github.com/HarperFast/ai-review-log/issues/699)). Nine points across the two bulk days, several author-fixed. This is the *mirror* of the inflation the existing "Severity discipline" bullet warns about, which is why it slipped.

2. **Fail-open / validate-at-registration on new surfaces (→ `universal.md`).** New registration/config/dispatch surfaces that silently degrade instead of failing closed: `defineBackend` assigning handlers on truthiness not function-ness → passes registration, crashes at call time (harper#1405, **@kriszyp, author-fixed**); a live-registry miss dispatching the base table class → row-level RBAC drops to table-level authz on create/update/delete, fail-*open* on the gate the PR adds (harper#1522, **@kylebernhardy**); a live reload to zero providers leaving a stale `enabled:true` MCP config (oauth#134, reproduced); `enc:v1` secrets silently skipped with no decryptor registered (harper#1528). Four points, human-corroborated + author-fixed.

3. **Secret leakage on the PR meant to protect secrets (→ `universal.md`).** `set_env_value` values not excluded from the operation log's `cleanBody` (harper#1527); a `clientSecret` in a `pluginDefaults` debug log never passed through `redactSecrets` (oauth#153) — **both author-fixed**. The flat "never log secrets" rule already exists but wasn't catching the specific mode: a new secret-bearing value not routed through the repo's existing redaction path.

_Corroborated but already covered (no edit):_ error/guard `TypeError`s masking the real error (harper#1474, #1521) sit under the existing **"Unvalidated shape"** robustness bullet; worker/subscription teardown races on 07-03 (harper-pro#445/#491, harper#1530) are covered by last week's **"Worker restart / pre-start ordering races"** + the existing setup/teardown and detached-async bullets. The lone severity-*inflation* partial ([#756](https://github.com/HarperFast/ai-review-log/issues/756), a coverage gap flagged as blocker, claude[bot]-only) and the gemini misfire citing files outside a test-only diff ([#780](https://github.com/HarperFast/ai-review-log/issues/780)) are single, uncorroborated points — no edit.

### Prompt changes this week

- **`universal.md` → "Output discipline" / Severity:** added **"Severity deflation on security / hardening paths — the mirror failure"** — a real, peer-flagged defect must not be talked down to a pass via unproven "unreachable" / "no escalation" / "safe" / "negligible" claims; the burden is on demonstrating unreachability, and a clean baseline paired with a peer finding the author then fixed is the signature. Anchored on author-fixed harper#1535 / #1528 / #1547 (partial class #1).
- **`universal.md` → "Security":** added **"Fail-closed / validate-at-registration on new surfaces"** — validate shape at the boundary and make fallbacks deny/throw, not proceed. Anchored on @kriszyp-fixed harper#1405, @kylebernhardy harper#1522, oauth#134, harper#1528 (FN class #2).
- **`universal.md` → "Security" / "Secret handling":** extended the existing bullet — on a PR that *adds* a secret-bearing field/op, verify the value is routed through the repo's existing redaction / `cleanBody` / debug-log-allowlist path, not left to a default that logs it verbatim. Anchored on author-fixed harper#1527 + oauth#153 (FN class #3).

No changes to `harper/common.md`, `harper/v5.md`, or `repo-type/plugin.md` — the three edited patterns are general security/robustness discipline (universal), and the Harper-core lifecycle patterns this week were already covered by prior additions.

## Week of 2026-06-22

Sources: [Calibration log #672](https://github.com/HarperFast/ai-review-log/issues/672) · [False-negative log #685](https://github.com/HarperFast/ai-review-log/issues/685)

### Verdict mix

**42 issues triaged across four daily sweeps: useful 35 · noise 3 · partial 4** (83% useful). A clean, useful-heavy week with one large mid-week sweep (06-23) carrying nearly all the signal.

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 06-22 | 3 | 3 | 0 | 0 |
| 06-23 | 35 | 29 | 2 | 4 |
| 06-24 | 3 | 2 | 1 | 0 |
| 06-25 | 1 | 1 | 0 | 0 |

(The false-negative log carried four daily scans, 06-23 → 06-26; the calibration log carried 06-22 → 06-25.)

### Recurring NOISE patterns (→ none to add)

All three noise calls are the same root mode already fully covered by the existing **"Mechanical, no-logic diffs"** bullet + the `<!-- review:no-log -->` marker: a vacuous "no blockers" on a diff with zero correctness surface — a one-line docs-URL string fix on a since-closed PR ([#680](https://github.com/HarperFast/ai-review-log/issues/680)) and a prose-only `CALIBRATION.md`/`universal.md` markdown change logged twice across 06-23/06-24 ([#670](https://github.com/HarperFast/ai-review-log/issues/670), itself a gemini review of a prompt PR — provider output, and doc-only regardless). No new suppression text warranted; the existing rule already names these shapes.

### Recurring FALSE-NEGATIVE patterns (→ add)

The week's meta-pattern is the now-familiar one: Claude posted a bare "no blockers" with nothing enumerated while a second reviewer (mostly gemini; one human) caught a real line-anchored defect — and on several, Claude affirmatively marked the very surface ("sort order", "no ordering race", `resolveResourcePath`) as verified-correct. Four crisp, corroborated sub-classes, each named by the daily triagers themselves:

1. **Numeric coercion and range on parsed/config/getter values** — the 06-23 triager's explicit "three misses share a type-coercion / uninitialized-value root." `getBlobReadTimeout()` not coerced → `Date.now() + …` string-concatenates and disables the timeout ([harper#1425](https://github.com/HarperFast/harper/pull/1425), **author-fixed**); validation before coercion → `Number(true) === 1` false-positive port-collision ([harper#1430](https://github.com/HarperFast/harper/pull/1430), **author-fixed**); an unvalidated `Number()` idle timeout of `Infinity`/`> 2147483647` coerced by `setTimeout` to ~1ms, instantly destroying a healthy stream ([harper#1458](https://github.com/HarperFast/harper/pull/1458), gemini). Three points, two author-fixed. The existing "Unguarded parse" bullet covers `JSON.parse`/`new URL`/`BigInt` *throwing* — not this *silent* coercion mode.
2. **Unhandled rejection on detached / fire-and-forget async** — the 06-24 triager's explicit "two of today's misses are unhandled promise rejections on detached/async paths." A `setImmediate`-deferred streaming IIFE with no `.catch()` ([harper#1404](https://github.com/HarperFast/harper/pull/1404), gemini) and `iterator?.return?.()` on watcher teardown returning a rejecting promise ([harper-pro#461](https://github.com/HarperFast/harper-pro/pull/461), **author-wrapped in `.catch()`**); the partial [#677](https://github.com/HarperFast/ai-review-log/issues/677) (iterator not closed on timeout → FD leak) corroborates the teardown half. Distinct from the existing "Error and abort paths → unhandled `'error'` event" bullet: there is no emitter to attach a listener to.
3. **`get()` is a `MaybePromise` consumed synchronously** (Harper-specific, data-loss, **human-corroborated**) — an unguarded sync read of a sibling `copyCursor` classified a valid cursor malformed on a block-cache miss (where `get()` returns a Promise), deleting it from disk and downgrading replication to a full recopy ([harper-pro#487](https://github.com/HarperFast/harper-pro/pull/487), **@DavidCockerill**; Claude reported "no blockers" and only acknowledged it after the fact). One very strong human/data-loss point; fits the file's existing single-incident human-corroborated precedent (harper#747/#890).
4. **Worker restart / pre-start ordering races** (author-corroborated, and an active mis-assertion) — three rolling-restart races (auto-restart of a deliberately-replaced worker, retry leaving both old+new alive, no startup timeout) in `manageThreads.js` ([harper#1432](https://github.com/HarperFast/harper/pull/1432), **author confirmed and fixed all three**), and three `SO_REUSEPORT` pre-start concurrency bugs that a review *explicitly declaring "no ordering race"* missed ([#668](https://github.com/HarperFast/ai-review-log/issues/668), **@kriszyp confirmed**). Two independent PRs, both human/author-corroborated.

Corroborating but already-covered (reinforce, no new text): generic-`500` misclassification advancing the resume cursor → silent data loss ([harper-pro#443](https://github.com/HarperFast/harper-pro/pull/443), [#661](https://github.com/HarperFast/ai-review-log/issues/661)) sits under the existing **Commit / replication source-apply atomicity** bullet; the `{ orderId }` shorthand treated as a full-scan ([harper#1427](https://github.com/HarperFast/harper/pull/1427)) and `undefined === undefined` test convergence ([harper-pro#442](https://github.com/HarperFast/harper-pro/pull/442), [#659](https://github.com/HarperFast/ai-review-log/issues/659)) are **Test validity** meta-check #1 items; the DRY_RUN-unguarded `execSync` deploy ([harper-pro#417](https://github.com/HarperFast/harper-pro/pull/417)) is a one-off script-safety miss. The **param-routing cluster** on [harper#1460](https://github.com/HarperFast/harper/pull/1460) (wildcard-position, registration-order route sort, trailing-slash mismatch — all gemini, author-fixed) is three findings on **one PR / one new feature** → single data point, watch-listed, no edit this week.

Signal weighting: edits anchor on the author-fixed (harper#1425, #1430, #1432) and human-corroborated (harper-pro#487 @DavidCockerill; harper#668 @kriszyp) points; the gemini-only points (harper#1458, #1404, harper-pro#461) reinforce classes the daily triagers independently named (06-23 type-coercion, 06-24 detached-async) rather than standing alone.

### Prompt changes this week

- **`universal.md` → "Robustness"**: added two bullets. (a) **"Numeric coercion and range on config / parsed / getter values"** — the silent (non-throwing) coercion mode: no coercion → string-concat disables a timeout; validate-before-coerce → `Number(true) === 1`; non-finite / out-of-range → `setTimeout` ~1ms overflow. Anchored on author-fixed harper#1425/#1430 + harper#1458 (FN class #1). (b) **"Unhandled rejection on detached / fire-and-forget async"** — `setImmediate`/`queueMicrotask` IIFEs, uncaught `.then()`, and promise-returning teardown (`iterator?.return?.()`) need a `.catch()`; explicitly distinguished from the emitter-`'error'` shape. Anchored on harper#1404 + author-fixed harper-pro#461 (FN class #2).
- **`harper/common.md` → "Non-obvious behavior"**: added **"`get()` is a `MaybePromise` — synchronous on a cache hit, a Promise on a miss"** — consuming the result synchronously (validate / `===` / destructure) misclassifies a valid value on the cache-miss path. Anchored on human-corroborated, data-loss harper-pro#487 (FN class #3).
- **`harper/common.md` → "Review checks specific to Harper"**: added **"Worker restart / pre-start ordering races"** — auto-restart of a deliberately-replaced worker, retry leaving old+new alive, untimed replacement startup, and pre-`listen` `SO_REUSEPORT` work; trace the exit-handler/replacement interplay rather than affirming "no ordering race." Anchored on author-fixed harper#1432 + @kriszyp-confirmed harper#668 (FN class #4).

No changes to `harper/v5.md` or `repo-type/plugin.md` this week — the week's new patterns are general JS/TS robustness (universal) and Harper-core lifecycle/storage (common); nothing v5-dispatch- or plugin-specific surfaced.

## Week of 2026-06-15

Sources: [Calibration log #577](https://github.com/HarperFast/ai-review-log/issues/577) · [False-negative log #559](https://github.com/HarperFast/ai-review-log/issues/559)

### Verdict mix

**121 issues triaged across six daily sweeps: useful 103 · noise 16 · partial 2.** A strongly useful-heavy week (85% useful).

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 06-16 | 31 | 24 | 6 | 1 |
| 06-17 | 2 | 1 | 0 | 1 |
| 06-18 | 37 | 33 | 4 | 0 |
| 06-19 | 26 | 21 | 5 | 0 |
| 06-20 | 24 | 23 | 1 | 0 |
| 06-21 | 1 | 1 | 0 | 0 |

### Recurring NOISE patterns (→ suppress)

The whole week's noise (≈13 of 16 calls) is the same root mode carried over from prior weeks — vacuous "no blockers" runs on PRs with **no reviewable surface**. Two sub-shapes recurred that the existing "Mechanical, no-logic diffs" bullet didn't yet name:

1. **Explicitly-throwaway PRs (by intent marker, not diff shape)** — the dominant new cluster, ≥7 data points across four sweeps and repeatedly recommended by the daily triagers as a pre-review gate: `[DIAGNOSTIC – do not merge]` / `[VALIDATION – do not merge]` canaries and draft "Not for merge" PRs ([#561](https://github.com/HarperFast/ai-review-log/issues/561), [#560](https://github.com/HarperFast/ai-review-log/issues/560), [#558](https://github.com/HarperFast/ai-review-log/issues/558), [#616](https://github.com/HarperFast/ai-review-log/issues/616), [#615](https://github.com/HarperFast/ai-review-log/issues/615), [#613](https://github.com/HarperFast/ai-review-log/issues/613)), and empty `+0/-0` investigation PRs ([#538](https://github.com/HarperFast/ai-review-log/issues/538)). The v24 LMDB-segfault debugging effort ([#616](https://github.com/HarperFast/ai-review-log/issues/616)/[#615](https://github.com/HarperFast/ai-review-log/issues/615)/[#613](https://github.com/HarperFast/ai-review-log/issues/613), all draft "Not for merge", since closed) was the tightest sub-cluster.
2. **CI-config-only diffs beyond pin bumps** — adding a `push:` trigger / no-op skip job so a required check passes ([#570](https://github.com/HarperFast/ai-review-log/issues/570), [#569](https://github.com/HarperFast/ai-review-log/issues/569)), a one-line `continue-on-error` workaround ([#615](https://github.com/HarperFast/ai-review-log/issues/615)). The existing bullet named CI *pin* bumps only.

Lower-frequency / watch-list (no edit): single-line `CONFIG_PARAMS` key registration where the feature lives elsewhere ([#581](https://github.com/HarperFast/ai-review-log/issues/581), 1 pt); doc-/prompt-prose-only PRs generating empty "no blockers" runs ([#555](https://github.com/HarperFast/ai-review-log/issues/555), [#633](https://github.com/HarperFast/ai-review-log/issues/633), [#602](https://github.com/HarperFast/ai-review-log/issues/602) — two are gemini reviews of prompt PRs); a cancelled/incomplete run logged as "no blockers" ([#557](https://github.com/HarperFast/ai-review-log/issues/557)). The lone partial ([#484](https://github.com/HarperFast/ai-review-log/issues/484)) is severity inflation on a test-only PR — already covered by the **Severity discipline** note.

### Recurring FALSE-NEGATIVE patterns (→ add)

The week's dominant, well-corroborated false-negative class is **error / abort / teardown paths** — Claude posted a bare "no blockers" while a second reviewer (mostly gemini, several author-fixed; a few human-corroborated — @kriszyp on [harper#1323](https://github.com/HarperFast/harper/pull/1323), @heskew on [harper-pro#361](https://github.com/HarperFast/harper-pro/pull/361)) caught a real crash or leak on the failure branch, often the very path the PR set out to harden. Two recurring shapes the existing Robustness section didn't yet name:

1. **Unhandled `'error'` event crashes the Node process** — a stream/socket `'error'` with no listener (or a listener *removed* after connect): [harper#1209](https://github.com/HarperFast/harper/pull/1209) (06-16), [harper#1386](https://github.com/HarperFast/harper/pull/1386) ×2 (06-20).
2. **Resource not released/rebuilt on the error or abort branch** — the 06-20 triager's explicit cluster ("four of six are resource-lifecycle/teardown bugs on error or abort paths"): per-disconnect listener leak ([harper#1386](https://github.com/HarperFast/harper/pull/1386)), orphaned `payload_blob` on aborted upload ([harper#1410](https://github.com/HarperFast/harper/pull/1410)), socket not `destroy()`ed on a TLS error ([harper#1314](https://github.com/HarperFast/harper/pull/1314)), TLS secure context not rebuilt on key hot-reload ([harper#1409](https://github.com/HarperFast/harper/pull/1409), author-fixed).

Corroborating but already-covered (reinforce, no new text): unguarded throw / null-deref on a commit/handshake path — [harper#1321](https://github.com/HarperFast/harper/pull/1321), [harper#1153](https://github.com/HarperFast/harper/pull/1153), [harper-pro#394](https://github.com/HarperFast/harper-pro/pull/394), [harper-pro#384](https://github.com/HarperFast/harper-pro/pull/384) (all author-fixed) sit under the existing **Unvalidated shape** / **Non-critical work on a critical path** bullets; un-timed warmup RPC ([harper-pro#398](https://github.com/HarperFast/harper-pro/pull/398)) under **Missing timeout on a remote await**; uncapped cursor decode DoS ([harper#1320](https://github.com/HarperFast/harper/pull/1320), fixed with a 512-char cap) is an input-bounds variant. The partial — Claude *noted* the renovate `automerge: true` supply-chain risk in run-notes but didn't call it a blocker ([#525](https://github.com/HarperFast/ai-review-log/issues/525), 1 pt) — is already addressed by the existing **"Surface a real finding; never bury it"** rule; one data point, watch-listed.

Signal weighting: the error/abort-path edit anchors on the author-fixed / human-corroborated points ([harper#1409](https://github.com/HarperFast/harper/pull/1409) author-fixed; [harper#1386](https://github.com/HarperFast/harper/pull/1386)/[#1209](https://github.com/HarperFast/harper/pull/1209)/[#1410](https://github.com/HarperFast/harper/pull/1410)/[#1314](https://github.com/HarperFast/harper/pull/1314) gemini-flagged on real defects) and on two triagers independently naming this the week's dominant cluster.

### Prompt changes this week

- **`universal.md` → "Robustness"**: added an **"Error and abort paths that crash or leak"** bullet — (1) an unhandled `'error'` event on a stream/socket/`EventEmitter` (incl. a listener removed after connect) crashes the process; (2) a socket / listener / file handle / persisted blob / TLS secure context acquired on the happy path must be released or rebuilt on the error or abort branch, not only on clean completion. Anchored on the week's error/abort-path misses (harper#1386, #1209, #1410, #1314, #1409).
- **`universal.md` → "What to ignore"**: (a) added an **"Explicitly-throwaway or empty-diff PRs"** bullet — `[DIAGNOSTIC]`/`[VALIDATION]` not-for-merge tags, draft "Not for merge" PRs, and empty `+0/-0` diffs route to the one-sentence pass + `<!-- review:no-log -->` marker (explicitly *not* gating on draft status alone); (b) extended the mechanical-diff bullet's "CI pin bumps" to other **inert CI-config diffs** (added trigger, no-op skip job, `continue-on-error` workaround), with an explicit carve-out keeping workflow diffs that change `permissions:`, add an untrusted-input trigger, or touch secrets / `run:`-install steps / job structure a reviewable security surface. Targets noise patterns #1 (≥7 pts, repeatedly human-recommended) and #2 (3 pts).

No changes to `harper/common.md`, `harper/v5.md`, or `repo-type/plugin.md` this week — the week's signal is general/cross-repo (harper, harper-pro, oauth), with no new Harper- or plugin-specific pattern.

## Week of 2026-06-08

Sources: [Calibration log #422](https://github.com/HarperFast/ai-review-log/issues/422) · [False-negative log #479](https://github.com/HarperFast/ai-review-log/issues/479)

### Verdict mix

**240 issues triaged across six daily sweeps: useful 209 · noise 24 · partial 7.**

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 06-08 | 1 | 0 | 1 | 0 |
| 06-09 | 120 | 118 | 2 | 0 |
| 06-10 | 40 | 36 | 4 | 0 |
| 06-11 | 5 | 5 | 0 | 0 |
| 06-13 | 73 | 49 | 17 | 7 |
| 06-14 | 1 | 1 | 0 | 0 |

(06-09 is a bulk auto-label sweep — 118 baseline `verdict:useful` "no blockers" entries; no 06-12 calibration comment.)

### Recurring NOISE patterns (→ suppress)

1. **Vacuous "no blockers" runs on no-reviewable-code diffs** — dominant: ~21 of 24 noise calls across four sweeps (06-08, 06-09, 06-10, 06-13). Every one is a mechanical diff with nothing to verify — version-string bumps ([#439](https://github.com/HarperFast/ai-review-log/issues/439), [#438](https://github.com/HarperFast/ai-review-log/issues/438)), dep/devDep/lockfile bumps ([#437](https://github.com/HarperFast/ai-review-log/issues/437), [#500](https://github.com/HarperFast/ai-review-log/issues/500), [#527](https://github.com/HarperFast/ai-review-log/issues/527), [#478](https://github.com/HarperFast/ai-review-log/issues/478)), CI action/pin bumps ([#465](https://github.com/HarperFast/ai-review-log/issues/465)–[#467](https://github.com/HarperFast/ai-review-log/issues/467), [#417](https://github.com/HarperFast/ai-review-log/issues/417)), a submodule-pointer-only CI vehicle ([#427](https://github.com/HarperFast/ai-review-log/issues/427)), docs-only ([#415](https://github.com/HarperFast/ai-review-log/issues/415)), scaffold READMEs ([#460](https://github.com/HarperFast/ai-review-log/issues/460)), and dead-code/lint removals ([#481](https://github.com/HarperFast/ai-review-log/issues/481), [#482](https://github.com/HarperFast/ai-review-log/issues/482)). The 06-09 and 06-10 triagers both explicitly recommend gating the review off these diff shapes. Strongly recurring + repeatedly human-recommended. (Per the convention update logged 2026-06-01 these are by-construction noise, but the *output* — a no-op log entry that must be hand-triaged — is what the prompt can shrink.)
2. **Severity inflation on test-only PRs** ([#484](https://github.com/HarperFast/ai-review-log/issues/484), [#485](https://github.com/HarperFast/ai-review-log/issues/485), [#503](https://github.com/HarperFast/ai-review-log/issues/503), 06-13) — a test-hygiene nit, a self-retracted false positive, and a documented intentional sequencing choice each headlined "blocker." Already covered by the **Severity discipline** note added 2026-06-01; reinforces, no new text.
3. **Gemini canned passes** ([#463](https://github.com/HarperFast/ai-review-log/issues/463), [#468](https://github.com/HarperFast/ai-review-log/issues/468), [#528](https://github.com/HarperFast/ai-review-log/issues/528)) — provider says "unable to generate a review," then posts no-blockers anyway. Gemini's output, not Claude's prompt — out of scope.

### Recurring FALSE-NEGATIVE patterns (→ add)

The meta-pattern all week: Claude posted a bare "Reviewed; no blockers found" with zero inline comments while gemini-code-assist (and, on a few, humans/Codex) caught real, often author-fixed correctness/data-integrity bugs — repeatedly on the very commit/replication paths the PRs set out to harden. Specific recurring, corroborated shapes:

1. **Setup/teardown leaks on partial failure** — `Promise.all` startup that orphans siblings on first reject, and teardown wired to state assigned only after setup completes (skipped when setup throws). ≥5 data points across 06-10/06-11/06-13, several author-fixed: [harper-pro#252](https://github.com/HarperFast/harper-pro/pull/252) (switched to `allSettled`), [#304](https://github.com/HarperFast/harper-pro/pull/304), [#297](https://github.com/HarperFast/harper-pro/pull/297), [harper#1227](https://github.com/HarperFast/harper/pull/1227) (`finally` throw skips `clearInterval`+exit → hung worker).
2. **Undefined-outside-expected-context property access** — missing null/undefined guards on objects that are undefined during early bootstrap, on a freshly-joined peer, or when invoked outside a REST request: [harper#1211](https://github.com/HarperFast/harper/pull/1211) (`context.response` in test), [harper-pro#326](https://github.com/HarperFast/harper-pro/pull/326) (`tables` on fresh peer), [harper-pro#360](https://github.com/HarperFast/harper-pro/pull/360) ×2 (`store` in bootstrap/tests, resolved), [harper#1273](https://github.com/HarperFast/harper/pull/1273) (hostname `validate` accepts whitespace). ≥5 points; extends the existing Robustness "Unvalidated shape" class.
3. **Commit / replication source-apply atomicity** (Harper-specific, high severity) — [harper#1252](https://github.com/HarperFast/harper/pull/1252) (four apply-path findings: sequence-id advanced past a failed txn, post-failed-commit `beginTxn` applied as standalone writes, sync `onCommit` throw marking a durable commit failed, uncapped retry) and [harper#1154](https://github.com/HarperFast/harper/pull/1154) (`HAS_STRUCTURE_UPDATE` flag set pre-commit-confirmation → spurious flag desyncs replication, **author-fixed**). The partial [#474](https://github.com/HarperFast/ai-review-log/issues/474) (missed mesh leak via `replicates.sends`) corroborates the same path.

Signal weighting: edits anchor on the author-fixed / human-corroborated points — the teardown class (#252/#304/#297/#1227, several author-fixed) and the commit-atomicity class (#1154 author-fixed, #1252 corroborated by #474). The input-guard class is mostly gemini-only but is a known recurring class (week-over-week) and several threads were resolved/acted on. The 06-12 four-finding cluster on #1252 is one PR/bot-only, so the Harper-commit edit is framed as a watch-list extension of the existing transaction bullet rather than a broad new rule.

### Prompt changes this week

- **`universal.md` → "What to ignore"** + **`.github/scripts/log-review-to-ai-review-log.sh`**: added a **mechanical, no-logic diffs** bullet (version-string-only / CI-pin / submodule-pointer / scaffold-README / dead-code-removal); on a wholly-non-reviewable diff the reviewer emits a `<!-- review:no-log -->` marker, and the logging step now skips creating a log entry when that marker is present — so no-code PRs actually stop generating triage-only entries (the prompt rule alone couldn't suppress them; the one-sentence pass is still posted and would otherwise be logged). Targets noise pattern #1 (~21 data points, twice human-recommended). _(Logging-step gate added during pre-merge review of this PR, beyond the routine's prompt-only scope.)_
- **`universal.md` → "Robustness"**: (a) extended the "Unvalidated shape" bullet to cover *internal* objects undefined outside their expected context (bootstrap store/handle, `tables` on a fresh peer, `context.response` outside REST) — FN pattern #2; (b) added a **"Setup/teardown that leaks on partial failure"** bullet — `Promise.all` sibling-orphaning (use `allSettled`/cleanup) and teardown skipped when setup throws before the handle is assigned — FN pattern #1.
- **`harper/common.md` → "Review checks specific to Harper"**: added a **"Commit / replication source-apply atomicity"** bullet (sequence-id skip past a failed txn, post-failed-commit standalone-write atomicity break, sync `onCommit` throw, pre-commit audit-flag set) as data-integrity blockers — FN pattern #3, anchored on author-fixed harper#1154 + harper#1252.

No changes to `harper/v5.md` or `repo-type/plugin.md` this week.

## Week of 2026-06-01

Sources: [Calibration log #372](https://github.com/HarperFast/ai-review-log/issues/372) · [False-negative log #371](https://github.com/HarperFast/ai-review-log/issues/371)

### Verdict mix

**201 issues triaged across five daily sweeps: useful 154 · noise 23 · partial 24.** The 06-02 sweep was a 156-issue backlog catch-up; the rest are normal daily volume.

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 06-02 (backlog) | 156 | 125 | 14 | 17 |
| 06-03 | 7 | 4 | 0 | 3 |
| 06-04 | 1 | 0 | 0 | 1 |
| 06-05 | 36 | 24 | 9 | 3 |
| 06-06 | 1 | 1 | 0 | 0 |

> **Verdict convention updated 2026-06-08 (mix recomputed).** Earlier sweeps counted every correct "no blockers" run as `noise`, which conflated true negatives (the reviewer correctly stayed silent on a real-code PR) with false positives (the reviewer was wrong) — corrupting the very signal-vs-noise metric this log exists to track. Under the current convention, a correct review that did substantive verification is `useful`; `noise` is reserved for vacuous runs (version/dependency/release bumps, formatting-only changes, file deletions, one-line CI pins) and genuinely wrong/irrelevant findings. The mix above is recomputed on that basis; the original tally was **useful 48 · noise 129 · partial 24**. The 23 remaining noise = 18 vacuous runs + the 3 Gemini false-positives ([#365](https://github.com/HarperFast/ai-review-log/issues/365), [#320](https://github.com/HarperFast/ai-review-log/issues/320), [#326](https://github.com/HarperFast/ai-review-log/issues/326)) + 2 cancelled runs.

### Recurring NOISE pattern (→ suppress)

**Severity inflation: cosmetic / by-design / accepted-trade-off findings promoted to "blocker."** The actionable miscalibration this week lives in the **partials**, not the noise. Under the updated convention the correct "no blockers" runs on real-code PRs are `useful` (they did substantive verification) — they carry no prompt-tuning signal but are not a defect. The recurring miss is consistently severity inflation by Claude's own reviews (human-corroborated — maintainers declined the suggestion or had already accepted the line):

- [#396](https://github.com/HarperFast/ai-review-log/issues/396) (oauth#116) — a cosmetic "skipped" CI-check entry on a label-only add flagged as a *blocker*; the caller-side label pre-filter was declined. Logged on both 06-04 and 06-05.
- [#394](https://github.com/HarperFast/ai-review-log/issues/394) (harper-pro#280) — a stale `# Runtime stage (UBI9)` comment after a `FROM`→ubi10 bump flagged as a blocker; real but cosmetic.
- [#384](https://github.com/HarperFast/ai-review-log/issues/384) (harper-pro#274) — `ENV HDB_ADMIN_PASSWORD=password` in the OpenShift image flagged as a blocker; pre-existing/by-design — identical line accepted in the main Dockerfile and the PR body itself flagged it ("Left as a default — flag if we'd rather drop it").

The 06-05 triager's own takeaway: *"tighten the blocker bar to correctness / security / API-contract only, route cosmetic and accepted-trade-off items to a non-blocking observations tier."* (Gemini's hallucinated finding-bearing reviews — [#365](https://github.com/HarperFast/ai-review-log/issues/365), [#320](https://github.com/HarperFast/ai-review-log/issues/320), [#326](https://github.com/HarperFast/ai-review-log/issues/326) — are out of scope here: they're Gemini's output, not Claude's prompt.)

### Recurring FALSE-NEGATIVE patterns (→ add)

1. **Async correctness — unawaited Promise lets the transaction commit before the work resolves** (human-corroborated, both CRITICAL):
   - `TableResource.update()` ignores the Promise from `_writeUpdate()` under `@embed` and returns `this` synchronously → transaction can commit before the embedder resolves → silent data loss or "Transaction already closed" ([harper#747](https://github.com/HarperFast/harper/pull/747), **@kriszyp CRITICAL**).
   - `evict()` returns a value from its `finally` block → swallows a `try` exception and overrides an early `return;`, resolving the Promise even when eviction was skipped ([harper#890](https://github.com/HarperFast/harper/pull/890), **@kriszyp**, same issue raised inline by gemini). Source: [#371 (06-02)](https://github.com/HarperFast/ai-review-log/issues/371).

2. **Defensive guards on external / cross-process / malformed input** — the dominant FN class, recurring week-over-week (the 06-03 note calls it "exactly the false-negative class flagged in last week's calibration"). Six gemini-caught misses in the 06-03 batch — `Object.entries`/optional-chain TypeErrors, `Int8Array`-vs-`Array.isArray`, an undefined `ws._socket`, a stale encoding offset, missing config bounds (harper#1098, #871, #872, harper-pro#255, #248) — plus the one human-corroborated data point this week: `purgeAgedLogs` added unguarded to the recovery/replay path, where a throw blocks log replay and startup ([harper#1117](https://github.com/HarperFast/harper/pull/1117), maintainer agreed, fixed `93a0060`). Source: [#371 (06-03, 06-04)](https://github.com/HarperFast/ai-review-log/issues/371).

Signal weighting: edits this week are anchored on the human-corroborated points — the async-correctness class (kriszyp, two CRITICAL findings) and the critical-path-cleanup miss (harper#1117, maintainer-fixed). The six 06-03 input-guard misses are gemini-only and largely already covered by the existing `universal.md` Robustness section (added last week); they reinforce the class rather than motivate new text. The single-cache-reference invalidation defect on oauth#106/#107 ([#371, 06-02](https://github.com/HarperFast/ai-review-log/issues/371)) was bot-only (gemini + the review bot) and is the same defect on a backport pair — noted, but not edited for, pending human corroboration.

### Prompt changes this week

- **`universal.md` → "What is NOT a blocker"**: added three explicit non-blocker bullets — cosmetic stale comments, by-design / pre-existing lines the PR didn't introduce (incl. the PR-body-flagged credential default and sibling-file precedent), and cosmetic CI-check-list / "skipped"-run noise — plus a **"Severity discipline"** note naming inflation (not omission) as the recurring failure mode and holding the blocker bar at correctness / security / API-contract / integrator-misleading docs only. Targets the week's severity-inflation partials (#396 ×2, #394, #384), all human-corroborated.
- **`harper/common.md` → "Review checks specific to Harper"**: added **"Async work must settle before the transaction commits"** — flag write/commit paths that ignore a returned Promise (returning `this` while `_writeUpdate()` is pending) as a correctness blocker, and the `finally`-returns-a-value footgun that swallows exceptions and overrides early returns. Anchored on the two kriszyp CRITICAL misses (harper#747, #890).
- **`universal.md` → "Robustness"**: added a **"Non-critical work on a critical path"** bullet — a non-essential operation (cleanup/prune/purge/metrics) injected into a critical lifecycle path (startup, recovery/replay, shutdown, commit) must be wrapped so a throw can't abort the critical path. Extends the existing recurring input-guard class with the one human-corroborated point this week (harper#1117).

No changes to `harper/v5.md` or `repo-type/plugin.md` this week.

## Week of 2026-05-25

Sources: [Calibration log #317](https://github.com/HarperFast/ai-review-log/issues/317) · [False-negative log #337](https://github.com/HarperFast/ai-review-log/issues/337)

### Verdict mix

**~36 distinct issues triaged: useful 17 · noise 16 · partial 3.**

The calibration log carried five daily comments, but two pairs are the same triage set re-summarized a day apart (the 05-28 set of 10, and the 25-issue set summarized on both 05-29 and 05-30 with identical issue numbers). De-duplicated, the week is three distinct sweeps:

| Sweep | Triaged | useful | noise | partial |
|-------|---------|--------|-------|---------|
| 05-28 set | 10 | 9 | 0 | 1 |
| 05-29 set (25) | 25 | 7 | 16 | 2 |
| 05-31 set | 1 | 1 | 0 | 0 |

### Recurring NOISE pattern (→ suppress)

**Forward-looking / out-of-diff observations promoted to findings on already-approved-or-merged PRs.** All 16 noise calls in the week share this single root mode — accurate but non-blocking run-notes (speculative follow-ups, cosmetic nits, caveats already in the PR body, intentional-by-design observations, "no reviewable surface" bumps) that read as findings at triage time. The triager's own downgrading language is the giveaway: "forward-looking notes, not blockers," "accurate context, not a finding," "speculative follow-up outside this diff." Strongly human-corroborated (16 independent triage calls). Source: [#317 comments of 05-29 / 05-30](https://github.com/HarperFast/ai-review-log/issues/317); noise issues [#186](https://github.com/HarperFast/ai-review-log/issues/186), [#194](https://github.com/HarperFast/ai-review-log/issues/194), [#197](https://github.com/HarperFast/ai-review-log/issues/197)–[#209](https://github.com/HarperFast/ai-review-log/issues/209), [#211](https://github.com/HarperFast/ai-review-log/issues/211), [#213](https://github.com/HarperFast/ai-review-log/issues/213).

### Recurring FALSE-NEGATIVE patterns (→ add)

1. **Defensive guards on external / cross-process / malformed input** — the strongest false-negative class, with author-fixed and human-corroborated data points:
   - Unvalidated `describe_all` response → `Object.keys()` `TypeError` crashes the schema clone ([harper-pro#237](https://github.com/HarperFast/harper-pro/pull/237), gemini HIGH, fixed `c75b398`).
   - Unguarded `new URL(leaderURL)` on malformed input → crash instead of heuristic fallback (harper-pro#237, gemini HIGH, fixed `c75b398`).
   - Falsy `!dbAuditSize` guard drops the metric for a legitimate `0` audit size ([harper#862](https://github.com/HarperFast/harper/pull/862), **@cb1kenobi human**, fixed `38c3f9d7`).
   - `broadcastDeployStart` awaits cross-thread ack with no timeout → one slow worker hangs the deploy indefinitely ([harper#822](https://github.com/HarperFast/harper/pull/822), gemini, author-fixed).
   - Backoff counter resets on any loop activity → tight 1s retry/log-spam loop defeats exponential backoff ([harper-pro#232](https://github.com/HarperFast/harper-pro/pull/232), gemini, twice).
   - Corroborating partial: the `node:undici`→`node:https` rewrite missed a request-timeout gap (`req.setTimeout`/`req.destroy`) that would hang on an unresponsive leader ([#142](https://github.com/HarperFast/ai-review-log/issues/142), **human-flagged**). Source: [#337](https://github.com/HarperFast/ai-review-log/issues/337) (05-29 / 05-30 / 05-31).

2. **Access-control *model* vs presence on MCP/Resource enumeration surfaces** — human-corroborated by **@kriszyp**, code changed as a result:
   - Resource enumeration gates on the caller's table permissions, but access is per-record via `allow{Read,…}` predicates — should key off REST-verb presence, not a permission walk ([harper#788](https://github.com/HarperFast/harper/pull/788)).
   - `exportTypes` not consulted during MCP enumeration → MCP surface wider than REST (harper#788).
   - `cluster_user` referenced in `canRoleInvokeOperation` is internal-only, never role-grantable → dead role check, subsequently dropped ([harper#781](https://github.com/HarperFast/harper/pull/781)). Claude verified the RBAC walks as a checked surface but missed the model-level error each time. Source: [#337](https://github.com/HarperFast/ai-review-log/issues/337) (05-29).

Signal weighting: the defensive-guard class mixes human/author-fixed (harper#862, #142) and bot-only-but-author-accepted (harper-pro#237, #232, harper#822) data points; the access-control class is human-corroborated (kriszyp) across two PRs that both resulted in code changes.

### Prompt changes this week

- **`universal.md` → "What is NOT a blocker"**: added an explicit bullet to suppress forward-looking / speculative / out-of-diff observations restated as findings, especially on already-approved-or-merged PRs (targets the week's dominant noise mode, 16/16 of the noise sweep).
- **`universal.md` → new "Robustness" section**: added checks for the external/cross-process-input failure edges that recurred as false negatives — unvalidated response shape (`Object.keys`/property access throws on a nullish value and silently misleads on a wrong-typed non-object), unguarded `new URL`/`JSON.parse`, falsy-vs-nullish guards that drop a legitimate `0`, missing timeout on a remote await, and backoff-reset-on-any-activity loops.
- **`harper/v5.md` → new "Access control: verify the model, not just its presence" subsection**: added the per-record-vs-list-time permission model, `exportTypes`/protocol gating, and grantable-role-identifier checks (kriszyp-corroborated MCP/Resource findings).
- **`universal.md` → "How to post" (forward-looking hypothesis)**: added a "surface a real finding; never bury it" rule — actionable concerns must appear as a finding, not demoted to a run-note / "new observation," and a `no blockers` summary must match the findings posted. **Placement is provider-aware**: inline review comment where the workflow supports one, otherwise the structured `**File:** path:line` form in the single top-level comment — so it doesn't conflict with the Gemini workflow's no-inline / one-top-level-comment contract (`_gemini-review.yml`). _This one is ahead of the weekly cadence: it's motivated by the 2026-06-01 triage batch (#214–238), not last week's logs — e.g. [#216](https://github.com/HarperFast/ai-review-log/issues/216), where a real, human-corroborated `sendToThread` silent-catch concern was logged as a non-actionable "new observation" rather than surfaced. Landed now to start gathering data; validate/refine against next week's calibration._

No changes to `repo-type/plugin.md` this week.

### Harper-awareness + public-skills dedup (this PR, not calibration-signal-driven)

Polish landed alongside the calibration so the review bots actually leverage the public `HarperFast/skills` they already check out (it was wired but latent — Claude treated it as an optional drill-down, Gemini's prompt never mentioned it):

- **`harper/common.md` → "Be Harper-aware — defer to the canonical best-practices"**: new note telling the reviewer to ground Harper-specific judgments in the bundled `harper-best-practices` rules (and treat a clear contradiction as a finding) rather than guessing. Path-agnostic, so it reaches both reviewers via composition.
- **`_claude-review.yml` / `_gemini-review.yml` prompts**: the Claude prompt's `.harper-skills/...` reference upgraded from optional-drill-down to an active "consult to ground Harper judgments"; the Gemini prompt — which previously didn't mention the skills at all — now points at `.harper-skills/harper-best-practices/rules/*.md`.
- **`harper/v5.md` → "Environment variables and `.env`"**: trimmed the re-explained `dotenv-cli` / `.env`-deploys-with-component mechanics down to a pointer into `harper-best-practices` (`creating-harper-apps.md`, `deploying-to-harper-fabric.md`), keeping only the review-lens checks (deploy-creds ≠ runtime-secrets; flag non-Fabric deploy advice). Removes the one spot the layer was duplicating the public skill.
- **`skills-ref` bumped `d2db99b` (1.3.0, 2026-04-16) → `7b57f1b` (v1.7.0, 2026-05-27)** in both reusable workflows — gets the reviews onto the latest `HarperFast/skills` release.
- **`harper/v5.md` → "config.yaml and resource loading"**: trimmed the restated `jsResource.files` / `graphqlSchema.files` loading mechanic to a pointer into `custom-resources.md` + `schema-design-tooling.md`, keeping the review checks.

**Public-skill dedup pass (scope: public `HarperFast/skills` only; private `skills-internal` rail intentionally parked).** All four layers were compared against all 20 `harper-best-practices` rules. The layers are a review lens with almost no verbatim overlap: the only spots that restated public-skill *mechanics* were the `.env`, Fabric-deploy, and `config.yaml` loading bits — all now pointers. Everything else Harper-specific (Resource API v2 static/instance dispatch duality, 405-vs-204, the access-control model, `GenericTrackedObject` spread, dev/prod dependency mismatch, plugin lifecycle) is review-depth with **no public-rule equivalent**, so it stays inline. Net: the public side is deduped; the private-rail (eng-guidelines / deep-review threat-model) decision remains open.
